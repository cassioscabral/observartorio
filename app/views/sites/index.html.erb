<h1>Listando links do portal:</h1><BR>

<table>
  <tr>
    <th>Links Cadastrados</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sites.each do |site| %>
  <tr>
    <td><%= site.nome_link_menu %></td>
    <td><%= link_to 'Visualizar', site %></td>
    <td><%= link_to 'Editar', edit_site_path(site) %></td>
    <td><%= link_to 'Apagar', site, method: :delete, data: { confirm: 'Tem certeza disso?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nova Página', new_site_path %>
